[project]
name = "ds-python-basics"
version = "0.1.0"
description = "Course materials for ds-python-basics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel"
]

[tool.uv]
package = false
